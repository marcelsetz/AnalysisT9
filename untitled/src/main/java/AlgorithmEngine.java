package nl.bioinf.cli_demo;

import java.io.File;

public class AlgorithmEngine {
    void start(OptionsProvider optionsProvider) {
        String inputFile = optionsProvider.getFileName();
        int number = optionsProvider.getNumber();
    }
}
